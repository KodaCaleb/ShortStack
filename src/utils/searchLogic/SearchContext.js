import React from "react";

// Create a new React context named "SearchContext"
const SearchContext = React.createContext();

// Export the "SearchContext" so that other components can use it
export default SearchContext;
